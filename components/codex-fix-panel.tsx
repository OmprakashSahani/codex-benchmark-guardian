import type { AnalysisResponse } from "@/lib/types";
import { ArtifactViewer, type Artifact } from "./artifact-viewer";

const steps = ["Regression detected", "Triage generated", "Codex prompt prepared", "Implementation fixed", "Benchmarks rerun"];

export function CodexFixPanel({ prompt }: { prompt: AnalysisResponse["codex_fix_prompt"] }) {
  const artifact: Artifact = {
    key: "codex_fix_prompt",
    title: "Codex investigation prompt",
    description: "Generated by the Python analysis API from this benchmark evidence.",
    filename: "codex-fix-prompt.txt",
    mime: "text/plain;charset=utf-8",
    language: "text",
    content: prompt,
  };

  return <section aria-labelledby="codex-fix-title" className="codex-fix-panel">
    <div className="detail-heading"><div><span className="eyebrow">Codex-ready investigation</span><h3 id="codex-fix-title">Turn benchmark evidence into an implementation task</h3><p>Copy the generated investigation prompt into Codex to identify the likely cause, implement a minimal fix, add coverage, and rerun the benchmark guardrail.</p></div></div>
    <ol className="codex-workflow" aria-label="Regression-to-fix workflow">{steps.map((step) => <li key={step}>{step}</li>)}</ol>
    <ArtifactViewer artifact={artifact} copyLabel="Copy for Codex" downloadLabel="Download prompt" />
  </section>;
}
